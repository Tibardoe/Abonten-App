diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index c080a58..66cdac3 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -119,18 +119,14 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
         if (enabledRef.current) {
             const url = getInitialURLRef.current();
             if (url != null) {
+                // Abonten patch: the launch URL is not recorded as an
+                // "unhandled link" here. Nothing in expo-router reads
+                // lastUnhandledLink, upstream react-navigation has removed this
+                // code, and on Android the URL is a promise that can resolve
+                // before the NavigationContainer's first commit — a React state
+                // update on a fiber that has not mounted (dev warning at boot).
                 if (typeof url !== 'string') {
-                    return url.then((url) => {
-                        const state = getStateFromURL(url);
-                        if (typeof url === 'string') {
-                            // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
-                        }
-                        return state;
-                    });
-                }
-                else {
-                    onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                    return url.then((url) => getStateFromURL(url));
                 }
             }
             state = getStateFromURL(url);
@@ -144,7 +140,7 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
             },
         };
         return thenable;
-    }, [getStateFromURL, onUnhandledLinking, prefixes]);
+    }, [getStateFromURL]);
     (0, react_1.useEffect)(() => {
         const listener = (url) => {
             if (!enabled) {
