// Re-export of the shared chat time helpers (see
// @abonten/core/messagingThread) so existing mobile imports stay stable.
export {
  clockTime,
  daySeparatorLabel,
  isSameCalendarDay,
  GROUPING_WINDOW_MS,
} from "@abonten/core/messagingThread";
